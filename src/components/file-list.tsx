
// This component is not used in the new WebRTC file sharing app.
// Kept minimal to avoid import errors if referenced elsewhere, will be removed or repurposed later.
'use client';

export function FileList() {
  return null;
}
